let x = 0;
let y = 0;

// controllers[0].axes

function setup() {
    createCanvas(400, 400);
}
  
function draw() {
    fill(51)
    background(220);
    rect(x+(width*0.5), y+(height*0.5), 10,10,15);
}
  